using ParkManagment.Entities;

namespace ParkManagment.Interfaces.Repositries
{
    public interface IEwalletRepository
    {
        // EWallet Create(EWallet wallet);
        // EWallet Update(EWallet wallet);
        // bool Delete(int id);
        // EWallet Get(int id);
    }
}